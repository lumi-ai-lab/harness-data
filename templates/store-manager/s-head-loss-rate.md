# 头部门店不良率指标分析报告模板

> 说明：本模板用于门店管理下门店盈利与运营效率维度中不良率链路的头部门店不良率专项报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。
> 品类口径固定为全品类，不输出品类排名、品类拖累或品类贡献。

## 通用填充规则

- 头部门店不良率是不良率（lossRate）的子指标，属于不良率分层指标组的叶子指标。
- 固定拆解链路：`门店净利润 -> 盈亏平衡点 -> 不良率 -> 头部门店不良率`。
- 本报告核心问题是判断头部门店不良率当前水平是否稳定可控，头部门店是否存在质量恶化风险，以及对整体不良率的影响。
- 不良率为反向指标，值越低越好；头部门店通常不良率最低，若异常上升需重点关注核心门店质量。
- 不良率 valueUnit=3（小数形式的比率），报告中需 ×100 转为百分比展示；同比/环比 unit=3 表示百分点变化。
- 门店净利润、盈亏平衡点、不良率只用于父级传导，不能替代当前指标。
- 其余不良率分层子指标（中上、中下、尾部）只可作为同组对比背景，不得替代当前指标。
- 营业门店数、坪效、人效、成本支出指标不得作为本报告主指标行。
- 区域、趋势证据只能用于解释头部门店不良率，不扩展为经营总览报告。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report store` 返回结果，围绕**头部门店不良率**在不良率分层中的表现、趋势变化和区域分化进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area/indicator filters or 全局>`
- 品类口径：全品类
- 数据来源：`report store indicators area trend`
- 总体判断：`<基于 CLI 证据概括头部门店不良率当前值、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕头部门店不良率当前值、同比环比变化方向和幅度>`
2. `<结论二：结合不良率和其余分层指标，说明头部门店在整体不良率结构中的位置>`
3. `<结论三：结合区域或趋势证据，指出需要重点关注的头部门店质量风险>`

---

## 三、父级链路与传导判断

| 层级 | 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 祖父指标 | 门店净利润 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<头部不良变化对门店净利润的传导>` |
| 父级指标 | 盈亏平衡点 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<头部不良变化对盈亏平衡点的传导>` |
| 父级指标 | 不良率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<头部不良变化对整体不良率的贡献>` |
| 当前指标 | 头部门店不良率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<头部不良强度判断>` |

---

## 四、头部门店不良率整体表现与趋势

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 头部门店不良率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<是否稳定可控、趋势方向判断>` |

### 趋势表现

- `<使用 trend 返回的最近周期、峰谷、连续变化或异常点证据>`

### 与不良率整体的对比

- `<头部门店不良率与整体不良率的差距，是否保持在合理范围>`
- `<与其他梯队（中上、中下、尾部）不良率的相对位置>`

---

## 五、区域分化

| 区域 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于全国均值、异常判断>` |

### 区域诊断

- `<头部门店不良率最高和最低的区域及差异>`
- `<是否存在某区域头部门店不良率异常偏高的情况>`

---

## 六、核心问题诊断与根因推断

### 1. `<问题一：来自 CLI 的头部门店不良率核心症状>`

- 现象：`<数值、同比、环比、排名或异常点>`
- 影响：`<对不良率整体、盈亏平衡点或门店净利润的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的区域或趋势风险>`

- 现象：`<区域分化、趋势拐点或连续异常>`
- 影响：`<对核心门店质量的潜在风险>`
- 推断：`<基于 CLI 证据的有限根因判断>`

---

## 七、行动建议与跟踪指标

- 头部门店管控：`<针对头部门店不良率的整治或监控动作，必须回指第六章问题>`
- 盈亏联动：`<针对盈亏平衡点和门店净利润证据提出动作>`
- 趋势预警：`<围绕趋势异常提出监控动作>`
- 跟踪指标：`<从 CLI 已返回指标中选择>`

---

> **附录：指标定义说明**

| 指标 | 核心定义 |
| :--- | :--- |
| 门店净利润 | 头部门店不良率所属链路的一级核心指标 |
| 盈亏平衡点 | 头部门店不良率所属链路的父级指标 |
| 不良率 | 头部门店不良率所属的分组父级指标 |
| 头部门店不良率 | 头部门店中不良门店的占比，反映核心门店质量健康度 |