# 人效指标分析报告模板

> 说明：本模板用于门店管理下的人效指标专项分析报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- 人效是门店管理一级核心指标门店净利润的下钻子指标，反映门店人力投入的产出效率（单人均净利润贡献）。
- 固定拆解链路：`门店净利润 -> 人效 -> 门店人数`。
- 本报告核心问题是判断人效当前水平是否达标（阈值 >= 2400），同比环比变化方向及幅度，变化是否来自区域结构分化或趋势异常，以及人效变化对门店盈利效率的整体影响。
- 区域、趋势证据只用于解释人效及其下钻链路，不扩展为门店管理总览报告。
- 营业门店数、开店数、闭店数、停业门店数不得作为本报告主指标行。
- 诊断必须基于已返回数据，使用"现象 -> 影响 -> 推断"的逻辑，不把通用业务常识写成已发生事实。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report store` 返回结果，围绕**人效**在门店管理中的整体表现、趋势变化、区域结构分化和可行动优化方向进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area/indicator filters or 全局>`
- 数据来源：`report store indicators area trend`
- 总体判断：`<基于 CLI 证据概括人效当前值、阈值达成、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕人效当前值（3673.58）、是否达标（阈值 >= 2400，已达标）、同比环比变化方向和幅度（环比 -206.68，同比 +936.12）>`
2. `<结论二：围绕区域分化，指出人效领先/拖累的主要区域>`
3. `<结论三：围绕趋势异常或结构性风险，指出需要重点关注的时间段或区域性问题>`

---

## 三、人效整体表现

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 人效 | 3673.58 | >= 2400 | -206.68（↓） | +936.12（↑） | `<diagnosis>` |

### 阈值判断

- 当前人效值 3673.58，阈值要求 >= 2400，已达标，超出阈值 1273.58。
- `<与上期对比的达标情况变化>`

### 趋势表现

- `<使用 trend 返回的最近周期变化方向（持续上升/下滑/震荡）、峰谷或异常点证据>`
- `<近30天趋势中是否存在显著拐点或连续多日偏离均值的情况>`

### 区域结构

- 区域表现：`<使用 area 返回的排名前/后区域、领先/拖累区域及其同比环比证据>`

---

## 四、区域表现拆解

| 区域 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area_name>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于全国均值、阈值达成判断>` |

### 区域结构诊断

- `<人效最高的区域及其驱动因素判断，人效最低的区域及其拖累因素判断>`
- `<区域间分化是否在扩大，是否存在显著区域间人效差距>`
- `<如有时序比较，指出哪些区域在改善、哪些在恶化>`

---

## 五、父子链路与传导分析

| 层级 | 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 当前 | 人效 | 3673.58 | >= 2400 | -206.68 | +936.12 | `<整体判断>` |
| 子指标 | 门店人数 | 5.26 | <= 5 | 0 | 0 | `<对人效的传导>` |

### 传导诊断

- `<判断人效变化的主要驱动：来自门店净利润变化还是门店人数变化>`
- `<当门店人数上升而净利润不变时，人效被稀释；当门店人数下降或净利润增长时，人效提升>`
- `<门店人数当前值（5.26）超出阈值（<= 5），对人效形成稀释压力，需关注人力配置合理性>`

---

## 六、核心问题诊断与根因分析

### 1. `<问题一：人效环比下降 206.68>`

- 现象：`<数值、阈值、同比、环比、排名或异常点>`
- 影响：`<对门店盈利效率、人力成本回报的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：门店人数超出阈值>`

- 现象：`<门店人数 5.26 > 阈值 5，超出 0.26，对人效形成稀释>`
- 影响：`<对人效分母的抬升作用，制约人效提升空间>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 3. `<问题三：区域人效分化或趋势风险>`

- 现象：`<趋势拐点、连续异常日、区域分化>`
- 影响：`<对短期和中期人效走势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

---

## 七、优化策略与行动建议

### 1. 人效提升（分子端：净利润驱动）

- 短期动作：`<针对人效环比下降的证据提出动作，重点在净利润端优化>`
- 长期动作：`<建立门店盈利能力基线，提升单店净利润以驱动人效增长>`
- 跟踪指标：人效、门店净利润

### 2. 人力配置优化（分母端：门店人数控制）

- 短期动作：`<针对门店人数超出阈值（5.26 > 5）提出动作，评估人力配置合理性和冗余>`
- 长期动作：`<建立分区域、分门店类型的人力配置标准，将门店人数控制在阈值以内>`
- 跟踪指标：人效、门店人数

### 3. 区域人效均衡

- 短期动作：`<针对人效落后区域提出动作，对标领先区域的人效运营模式>`
- 长期动作：`<建立区域人效对标机制，对持续低于全国均值 80% 的区域设置专项提升计划>`
- 跟踪指标：人效（分区域）、门店人数（分区域）

---

## 八、风险提示与后续跟踪

- 风险提示：`<只列由 CLI 证据支持的人效、门店人数、区域分化或趋势风险>`
- 后续跟踪：`<明确后续应跟踪的已返回指标、区域或时间窗口>`
- 重点关注：`<门店人数超阈值对人效的稀释风险、环比下降趋势是否持续、区域间人效差距扩大风险>`

---

## 九、附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| 人效 | 门店单人均净利润贡献（门店净利润 / 门店人数），反映门店人力投入的产出效率 |
| 门店人数 | 统计周期内门店平均在岗人员数量，是人效计算的分母，人数越低越有利于人效提升 |
| 门店净利润 | 人效的父指标，门店管理一级核心指标，反映门店整体盈利水平 |

> 数据来源优先级：`qdm-cmr-cli report store indicators > area > trend`。所有数值必须在 CLI 返回结果中有据可查。