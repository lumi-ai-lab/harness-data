# 店铺水电费指标分析报告模板

> 说明：本模板用于门店管理下的店铺水电费指标专项分析报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- 店铺水电费是门店管理盈亏平衡点费用组（`isGroup: true`，`layout: vertical`）的组成指标之一。
- 费用组兄弟指标：员工工资、租金和物业管理费、门店其他支出。
- 本报告核心问题是判断店铺水电费当前水平是否在阈值范围内，同比环比变化方向及幅度，变化是否来自区域结构分化或趋势异常。
- 区域、趋势证据只用于解释店铺水电费及其在费用组中的位置，不扩展为经营总览报告。
- 门店净利润、坪效、人效等非费用类指标不得作为本报告主指标行。
- 诊断必须基于已返回数据，使用"现象 -> 影响 -> 推断"的逻辑，不把通用业务常识写成已发生事实。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report store` 返回结果，围绕**店铺水电费**在门店管理中的整体表现、趋势变化、区域结构分化和可行动优化方向进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area filters or 全局>`
- 数据来源：`report store indicators area trend`
- 总体判断：`<基于 CLI 证据概括店铺水电费当前值、阈值达成、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕店铺水电费当前值、是否在阈值范围内（<=150）、同比环比变化方向和幅度>`
2. `<结论二：围绕区域分化，指出店铺水电费偏高或偏低的区域>`
3. `<结论三：围绕趋势异常或结构性风险，指出需要重点关注的时间段或结构性问题>`

---

## 三、店铺水电费整体表现

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 店铺水电费 | `<value>` | `<threshold: <=150>` | `<mom>` | `<yoy>` | `<diagnosis>` |

### 阈值判断

- `<是否在阈值范围内（店铺水电费阈值 <=150），超出时说明超出幅度>`
- `<与上期对比的达标情况变化>`

### 趋势表现

- `<使用 trend 返回的最近周期变化方向（持续上升/下滑/震荡）、峰谷或异常点证据>`
- `<近30天趋势中是否存在显著拐点或连续多日偏离均值的情况>`

### 区域结构

- 区域表现：`<使用 area 返回的排名前/后区域、领先/拖累区域及其同比环比证据>`

---

## 四、区域表现拆解

| 区域 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area_name>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于全国均值、阈值达成判断>` |

### 区域结构诊断

- `<店铺水电费最高的区域及其驱动因素判断，店铺水电费最低的区域及其特征>`
- `<区域间分化是否在扩大，是否存在显著区域间店铺水电费差距>`
- `<如有时序比较，指出哪些区域在改善、哪些在恶化>`

---

## 五、核心问题诊断与根因分析

### 1. `<问题一：来自 CLI 的店铺水电费核心症状>`

- 现象：`<数值、阈值、同比、环比、排名或异常点>`
- 影响：`<对门店成本控制或盈亏平衡的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的区域结构性差异>`

- 现象：`<区域分化、排名异常或集中度变化>`
- 影响：`<对整体店铺水电费水平的拖累或拉升幅度>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 3. `<问题三：来自 CLI 的趋势风险或异常波动>`

- 现象：`<趋势拐点、连续异常日、波动率变化>`
- 影响：`<对短期和中期店铺水电费走势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

---

## 六、优化策略与行动建议

### 1. 水电能耗控制

- 短期动作：`<针对店铺水电费超阈值或偏高区域证据提出动作，必须回指第五章问题>`
- 长期动作：`<节能设备投入、能耗标准管控、门店能耗审计等机制>`
- 跟踪指标：店铺水电费、盈亏平衡点、门店净利润

### 2. 费用结构优化

- 短期动作：`<针对店铺水电费在费用组中的占比证据提出动作>`
- 长期动作：`<费用结构优化、门店效益联动机制>`
- 跟踪指标：店铺水电费、员工工资、租金和物业管理费、门店其他支出、盈亏平衡点

---

## 七、风险提示与后续跟踪

- 风险提示：`<只列由 CLI 证据支持的店铺水电费、成本或结构风险>`
- 后续跟踪：`<明确后续应跟踪的已返回指标、区域或时间窗口>`
- 重点关注：`<需要持续监控的指标阈值、区域或费用项>`

---

## 八、附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| 店铺水电费 | 门店水电费用合计（万元），统计周期内门店水费与电费合计，反映门店能耗成本的规模水平 |
| 员工工资 | 门店员工工资总额（万元），统计周期内门店员工工资合计 |
| 租金和物业管理费 | 门店租金及物业管理费用合计（万元），统计周期内门店租金与物业管理费合计 |
| 门店其他支出 | 门店除工资、租金、水电外的其他运营支出合计（万元），统计周期内门店其他运营支出合计 |
| 盈亏平衡点 | 门店净利润为零时的盈亏平衡状态，费用组指标汇总反映门店成本结构 |