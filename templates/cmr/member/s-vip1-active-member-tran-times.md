# vip1活跃会员消费频次指标分析报告模板

> 说明：本模板用于用户报表下的 vip1活跃会员消费频次指标专项分析报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- vip1活跃会员消费频次是用户规模与分层结构维度下"会员分层活跃与价值指标"组的叶子指标。
- 父指标：vip1活跃会员数（`vip1ActiveMemberNum`）。
- 无子指标。
- 本报告核心问题是判断 vip1活跃会员消费频次当前水平是否合理，同比环比变化方向及幅度，变化是否来自区域结构分化或趋势异常，以及消费频次变化对 VIP1 会员活跃度和忠诚度的整体影响。
- 区域、趋势证据只用于解释 vip1活跃会员消费频次表现，不扩展为用户运营总览报告。
- 会员价值与复购转化指标（会员复购率、会员销售占比等）、用户触达与渠道效率指标（可触达用户数等）不得作为本报告主指标行。
- 用户报表不支持品类过滤；不输出品类排名、品类拖累或品类贡献。
- 诊断必须基于已返回数据，使用"现象 -> 影响 -> 推断"的逻辑，不把通用业务常识写成已发生事实。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report user` 返回结果，围绕 **vip1活跃会员消费频次** 在用户规模与分层结构中的整体表现、趋势变化、区域结构分化和可行动优化方向进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area filters or 全国不含港澳>`
- 数据来源：`report user indicators area trend`
- 总体判断：`<基于 CLI 证据概括 vip1活跃会员消费频次当前值、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕 vip1活跃会员消费频次当前值、同比环比变化方向和幅度>`
2. `<结论二：围绕区域分化，指出消费频次主要领先/落后的区域>`
3. `<结论三：围绕趋势异常或结构性风险，指出需要重点关注的时间段或结构性问题>`

---

## 三、vip1活跃会员消费频次整体表现

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| vip1活跃会员消费频次 | `<value>次` | `<无阈值>` | `<mom>` | `<yoy>` | `<diagnosis>` |

### 消费频次判断

- `<vip1活跃会员消费频次当前水平评估，与其他 VIP 层级对比>`
- `<与上期对比的消费频次变化方向>`

### 趋势表现

- `<使用 trend 返回的最近周期变化方向（持续上升/下滑/震荡）、峰谷或异常点证据>`
- `<近30天趋势中是否存在显著拐点或连续多日偏离均值的情况>`

### 区域结构

- 区域表现：`<使用 area 返回的排名前/后区域、领先/拖累区域及其同比环比证据>`

---

## 四、区域表现拆解

| 区域 | 消费频次 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area_name>` | `<value>次` | `<mom>` | `<yoy>` | `<高于/低于全国均值、消费活跃度判断>` |

### 区域结构诊断

- `<vip1活跃会员消费频次最高的区域及其驱动因素判断，消费频次最低的区域及其特征>`
- `<区域间分化是否在扩大，是否存在显著区域间差距>`
- `<如有时序比较，指出哪些区域在改善、哪些在恶化>`

---

## 五、核心问题诊断与根因分析

### 1. `<问题一：来自 CLI 的 vip1活跃会员消费频次核心症状>`

- 现象：`<数值、同比、环比、排名或异常点>`
- 影响：`<对 VIP1 会员活跃度或忠诚度的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的区域消费频次分化>`

- 现象：`<区域分化、排名异常或集中度变化>`
- 影响：`<对整体 vip1活跃会员消费频次的拖累幅度>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 3. `<问题三：来自 CLI 的趋势风险或异常波动>`

- 现象：`<趋势拐点、连续异常日、波动率变化>`
- 影响：`<对短期和中期 vip1活跃会员消费频次走势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

---

## 六、优化策略与行动建议

### 1. VIP1 会员消费频次提升

- 短期动作：`<针对消费频次证据提出动作，必须回指第五章问题>`
- 长期动作：`<促销频次策略、会员日运营、品类推荐、复购激励等机制>`
- 跟踪指标：vip1活跃会员消费频次、vip1活跃会员数

### 2. 区域消费频次差异化优化

- 短期动作：`<针对区域消费频次分化证据提出动作>`
- 长期动作：`<区域差异化消费激励策略、低消费频次区域专项运营机制>`
- 跟踪指标：vip1活跃会员消费频次、vip1活跃会员数

### 3. VIP1 会员粘性增强

- 短期动作：`<针对消费频次趋势下滑证据提出动作>`
- 长期动作：`<会员积分体系、定期消费提醒、专属优惠机制>`
- 跟踪指标：vip1活跃会员消费频次、vip1活跃会员客单价

---

## 七、风险提示与后续跟踪

- 风险提示：`<只列由 CLI 证据支持的 vip1活跃会员消费频次下滑、区域分化扩大或趋势异常风险>`
- 后续跟踪：`<明确后续应跟踪的已返回指标、区域或时间窗口>`
- 重点关注：`<需要持续监控的消费频次拐点、区域或趋势异常>`

---

## 八、附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| vip1活跃会员消费频次 | 会员等级为vip1的活跃会员，平均每位会员在统计周期内的消费次数；统计逻辑为 vip1活跃会员来客数 / vip1活跃会员消费人数 |
| vip1活跃会员数 | 会员等级为VIP1的活跃会员id去重计数，反映低等级VIP用户规模；单位为万 |
| vip1活跃会员客单价 | VIP1活跃会员的平均每笔消费金额，反映低等级VIP消费价值 |
| 活跃用户数 | 有消费行为的用户id去重计数，反映整体用户规模；单位为万 |