# 尾部门店不良率指标分析报告模板

> 说明：本模板用于门店管理下门店盈利与运营效率维度中不良率链路的尾部门店不良率专项报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。
> 品类口径固定为全品类，不输出品类排名、品类拖累或品类贡献。

## 通用填充规则

- 尾部门店不良率是不良率（lossRate）的子指标，属于不良率分层指标组的叶子指标。
- 固定拆解链路：`门店净利润 -> 盈亏平衡点 -> 不良率 -> 尾部门店不良率`。
- 本报告核心问题是判断尾部门店不良率当前水平是否过高，是否存在持续恶化趋势，以及尾部门店的退出或整改优先级。
- 不良率为反向指标，值越低越好；尾部门店不良率通常显著偏高，为门店退出、整改或资源重配的主要决策依据。
- 不良率 valueUnit=3（小数形式的比率），报告中需 ×100 转为百分比展示；同比/环比 unit=3 表示百分点变化。
- 门店净利润、盈亏平衡点、不良率只用于父级传导，不能替代当前指标。
- 其余不良率分层子指标（头部、中上、中下）只可作为同组对比背景，不得替代当前指标。
- 营业门店数、坪效、人效、成本支出指标不得作为本报告主指标行。
- 区域、趋势证据只能用于解释尾部门店不良率，不扩展为经营总览报告。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report store` 返回结果，围绕**尾部门店不良率**在不良率分层中的表现、趋势变化和区域分化进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area/indicator filters or 全局>`
- 品类口径：全品类
- 数据来源：`report store indicators area trend`
- 总体判断：`<基于 CLI 证据概括尾部门店不良率当前值、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕尾部门店不良率当前值、同比环比变化方向和幅度>`
2. `<结论二：结合不良率和其余分层指标，说明尾部梯队对整体不良率的拖累程度和退出/整改紧迫性>`
3. `<结论三：结合区域或趋势证据，指出尾部门店集中的区域和需要优先退出的门店群体>`

---

## 三、父级链路与传导判断

| 层级 | 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 祖父指标 | 门店净利润 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<尾部不良变化对门店净利润的传导>` |
| 父级指标 | 盈亏平衡点 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<尾部不良变化对盈亏平衡点的传导>` |
| 父级指标 | 不良率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<尾部不良变化对整体不良率的拖累>` |
| 当前指标 | 尾部门店不良率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<尾部不良严重程度判断>` |

---

## 四、尾部门店不良率整体表现与趋势

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 尾部门店不良率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<是否过高、持续恶化判断>` |

### 趋势表现

- `<使用 trend 返回的最近周期、峰谷、连续变化或异常点证据>`

### 与不良率整体的对比

- `<尾部门店不良率与整体不良率的差距，对整体不良率的贡献度>`
- `<尾部梯队不良率与中下梯队不良率的差距是否在扩大>`
- `<尾部门店数量占比和不良率水平是否构成门店网络的结构性拖累>`

---

## 五、区域分化

| 区域 | 当前值 | 环比变动 | 同比变动 | 尾部门店数占比 | 诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `<area>` | `<value>` | `<mom>` | `<yoy>` | `<占比（如有）>` | `<退出/整改紧迫性判断>` |

### 区域诊断

- `<尾部门店不良率最高和最低的区域及差异>`
- `<是否存在某区域尾部不良率过高的结构性问题，是否有区域尾部门店集中的情况>`

---

## 六、核心问题诊断与根因推断

### 1. `<问题一：来自 CLI 的尾部门店不良率核心症状>`

- 现象：`<数值、同比、环比、排名或异常点>`
- 影响：`<对不良率整体、盈亏平衡点或门店净利润的严重拖累>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的尾部退出或整改优先级>`

- 现象：`<尾部不良率绝对值过高、持续上升趋势、区域集中等>`
- 影响：`<对门店网络效率和资源配置的影响>`
- 推断：`<基于 CLI 证据判断哪些尾部门店应优先退出或整改>`

---

## 七、行动建议与跟踪指标

- 尾部门店退出机制：`<针对尾部不良率过高门店的退出或关停动作，必须回指第六章问题>`
- 尾部门店整改：`<针对尚有改善空间的尾部门店的整改动作和考核标准>`
- 资源重配：`<将尾部门店释放的资源向头部和中上梯队倾斜的动作>`
- 盈亏联动：`<针对盈亏平衡点和门店净利润证据提出动作>`
- 跟踪指标：`<从 CLI 已返回指标中选择>`

---

> **附录：指标定义说明**

| 指标 | 核心定义 |
| :--- | :--- |
| 门店净利润 | 尾部门店不良率所属链路的一级核心指标 |
| 盈亏平衡点 | 尾部门店不良率所属链路的父级指标 |
| 不良率 | 尾部门店不良率所属的分组父级指标 |
| 尾部门店不良率 | 尾部门店中不良门店的占比，为门店退出和整改的主要决策依据 |