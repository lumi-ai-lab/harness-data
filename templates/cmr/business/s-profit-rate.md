# 门店毛利率指标分析报告模板

> 说明：本模板用于经营分析下客数渗透率树中的门店毛利率专项报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- 固定拆解链路：`客数渗透率 -> 全链路毛利率 -> 门店毛利率`。
- 本报告核心问题是判断门店端毛利率表现，并说明它对全链路毛利率和客数渗透率的传导影响。
- 全链路毛利率和客数渗透率只用于父级传导，供应链毛利率只作为同组补充，不替代当前指标。
- 门店毛利额、全链路毛利额、供应链毛利额不得作为本报告主指标行。
- 销售额、客数、客单价、19点前链路指标不得作为本报告主指标行。
- 品效维度指标（品效、商品订购渗透率、定价毛利率、售价价格指数等）不得放入本章节作为主指标行。
- 供应链三率（准确率、准点率、合格率）、活跃供应商数、集采入库占比不得放入本章节作为主指标行。
- 本指标固定归入经营分析报告第三章（用户渗透维度深度拆解），禁止放入第四章和第五章。
- 区域、品类、趋势证据只用于解释门店毛利率结构分化和异常，不扩展为经营总览报告。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report business` 返回结果，围绕**门店毛利率**在客数渗透率树中的表现、阈值达成、父级传导、区域品类结构分化和趋势变化进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area/category/indicator filters or 全局>`
- 数据来源：`report business indicators area category trend`
- 总体判断：`<基于 CLI 证据概括门店毛利率当前值、阈值、同比、环比和主要变化方向；引用实际 CLI 数据示例：全国门店毛利率 21.30%，高于阈值 20.05%，环比 +2.04pp，同比 -2.21pp>`

## 二、核心结论

1. `<结论一：围绕门店毛利率当前值、阈值是否达标（>=20.05%）、同比环比变化方向和幅度>`
2. `<结论二：围绕区域和品类结构分化，指出主要领先/拖累的区域（如粤东 22.43% vs 运营直管 17.46%）和品类（如猪肉 33.44% vs 肉禽蛋 14.71%）>`
3. `<结论三：结合全链路毛利率说明门店端毛利对父级指标的传导（门店毛利率占全链路毛利率主体，约 72.6%）>`

## 三、父级链路与传导判断

| 层级 | 指标 | 当前值 | 阈值 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 上级指标 | 客数渗透率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<父级结果判断，是否达标对规模效应的限制>` |
| 父指标 | 全链路毛利率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<全链路毛利率传导判断，门店端与供应链端谁主驱动>` |
| 当前指标 | 门店毛利率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<门店端毛利率判断，是否构成全链路主体支撑>` |

- 门店毛利率占全链路毛利率的比重说明：`<计算门店毛利率 / 全链路毛利率，CLI 示例约 72.6%>`
- 门店与供应链毛利率变动方向是否一致、谁主导全链路变化：`<环比门店 +2.04pp vs 供应链 +0.26pp，门店主导；同比门店 -2.21pp vs 供应链 -1.00pp，门店拖累更大但供应链放大了全链路同比压力>`

## 四、门店毛利率整体表现与趋势（含区域品类结构分化）

### 4.1 整体表现

| 指标 | 当前值 | 阈值 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 门店毛利率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<diagnosis>` |

- 阈值判断：`<是否达到阈值（门店毛利率阈值 >=20.05%），未达标时说明差距幅度>`
- 趋势表现：`<使用 trend 返回的最近周期、峰谷、连续变化或异常点证据；CLI 示例：近 30 天区间 17.85%~23.51%，05/17 峰值 23.51%，05/20 低谷 17.85%>`
- 与去年同期趋势对比：`<使用 trend compare 数据判断当前 vs 去年同期多数时段是高/低，CLI 示例：30 天中 25 天当前值高于同期比较值>`

### 4.2 区域结构分化

| 区域 | 门店毛利率 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于全国均值、阈值达成判断>` |

- 区域间分化判断：`<粤东最高、运营直管最低，分化幅度及是否扩大>`
- 环比改善/恶化区域：`<驱动环比改善的主要区域，拖累环比的主要区域>`
- 同比压力来源：`<同比降幅最大的区域及其结构性影响>`

### 4.3 品类结构分化

| 品类 | 门店毛利率 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<category>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于整体均值、对整体门店毛利率的贡献判断>` |

- 品类间分化判断：`<猪肉最高、肉禽蛋最低，品类间天然毛利结构差异>`
- 环比改善/恶化品类：`<水果环比 +6.79pp 是否为季节性因素，预制菜环比唯一下降需关注>`
- 同比压力品类：`<蔬菜同比 -3.56pp、预制菜 -3.31pp，是否结构性拖累>`

## 五、核心问题诊断与根因推断

### 1. `<问题一：来自 CLI 的门店毛利率核心症状>`

- 现象：`<数值、阈值、同比、环比、排名或异常点>`
- 影响：`<对全链路毛利率或客数渗透率的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的区域或品类结构性拖累>`

- 现象：`<区域/品类分化、排名异常或集中度变化>`
- 影响：`<对门店毛利率和父指标传导的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 3. `<问题三：来自 CLI 的趋势异常或风险>`

- 现象：`<趋势拐点、连续异常日、波动率变化>`
- 影响：`<对短期和中期门店毛利率走势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

## 六、行动建议与跟踪指标

- 门店毛利修复：`<针对门店毛利率、区域或品类证据提出动作，必须回指第五章问题>`
- 区域重点管理：`<针对拖累区域（如运营直管）或结构分化提出动作>`
- 品类策略调整：`<针对同比压力大的品类（如蔬菜、预制菜）提出动作>`
- 全链路毛利承接：`<针对全链路毛利率证据提出动作>`
- 跟踪指标：`<从 CLI 已返回指标中选择：门店毛利率、全链路毛利率、供应链毛利率>`

## 七、附录：指标定义说明

| 指标 | 核心定义 | CLI 取值示例（2026-05-24） |
| :--- | :--- | :--- |
| 客数渗透率 | 门店毛利率所属的经营分析一级指标 | 26.46%，阈值 >=30%，未达标 |
| 全链路毛利率 | 门店毛利率所属的父级毛利率指标 | 29.34%，阈值 >27.9%，达标 |
| 门店毛利率 | 门店端毛利率表现，= 门店毛利额 / 销售额 | 21.30%，阈值 >=20.05%，达标 |
| 供应链毛利率 | 同组全链路盈利补充指标 | 10.59%，阈值 >11.18%，未达标 |