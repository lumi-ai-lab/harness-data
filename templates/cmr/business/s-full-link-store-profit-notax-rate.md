# 全链路毛利率指标分析报告模板

> 说明：本模板用于经营分析下客数渗透率树中的全链路毛利率专项分析报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- 全链路毛利率是客数渗透率树中连接规模类指标与全链路盈利指标的核心中间指标，反映门店与供应链综合后的整体毛利率水平。
- 固定拆解链路：`客数渗透率 -> 全链路毛利率 -> 门店毛利率、供应链毛利率`。
- 本报告核心问题是判断全链路毛利率当前水平是否达标，同比环比变化方向及幅度，变化来自门店侧还是供应链侧驱动，以及是否存在区域或品类结构分化。
- 区域、品类、趋势证据只用于解释全链路毛利率及其下钻链路，不扩展为经营总览报告。
- 销售额、客数、客单价、19点前链路指标、品效维度指标、供应链三率（准确率、准点率、合格率）、活跃供应商数、集采入库占比不得作为本报告主指标行。
- 全链路毛利率 `valueUnit: 3`（小数比率），显示时需 x100 转为百分比；同比环比 `unit: 3`（百分点变化，小数形式）。
- 诊断必须基于已返回数据，使用"现象 -> 影响 -> 推断"的逻辑，不把通用业务常识写成已发生事实。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report business` 返回结果，围绕**全链路毛利率**在客数渗透率树中的整体表现、门店与供应链毛利分化、区域品类结构分化和可行动优化方向进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area/category/indicator filters or 全局>`
- 数据来源：`report business indicators area category trend`
- 总体判断：`<基于 CLI 证据概括全链路毛利率当前值（如 29.34%）、阈值达成（>27.9%）、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕全链路毛利率当前值、是否达标（阈值 >27.9%）、同比环比变化方向和幅度>`
2. `<结论二：指出门店毛利率和供应链毛利率中的主要驱动或拖累方，及对全链路毛利率的传导效果>`
3. `<结论三：围绕区域和品类分化，指出领先/拖累区域和品类，以及趋势异常或结构性风险>`

---

## 三、全链路毛利率整体表现

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 全链路毛利率 | `<value>` | `<threshold: >27.9%>` | `<mom>` | `<yoy>` | `<diagnosis>` |

### 阈值判断

- `<是否达到阈值（全链路毛利率需 >27.9%），未达标时说明差距幅度>`
- `<与上期对比的达标情况变化>`

### 趋势表现

- `<使用 trend 返回的最近30天变化方向（持续上升/下滑/震荡）、峰谷或异常点证据>`
- `<近30天趋势中是否存在显著拐点或连续多日偏离均值的情况>`

### 区域与品类结构

- 区域表现：`<使用 area 返回的排名前/后区域、领先/拖累区域及其同比环比证据>`
- 品类表现：`<使用 category 返回的排名前/后品类、领先/拖累品类及其同比环比证据>`

---

## 四、门店与供应链毛利拆解

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 全链路毛利率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<利润质量整体判断>` |
| 门店毛利率 | `<value>` | `<threshold: >=20.05%>` | `<mom>` | `<yoy>` | `<门店侧利润贡献判断>` |
| 供应链毛利率 | `<value>` | `<threshold: >11.18%>` | `<mom>` | `<yoy>` | `<供应链侧利润贡献判断>` |

### 结构诊断

- `<判断全链路毛利率变化主要来自门店侧还是供应链侧的驱动>`
- `<说明两侧毛利率是否同向变化，若分化则说明分化程度和方向>`
- `<引用门店毛利率和供应链毛利率各自的阈值达成情况，说明各自对全链路毛利率的贡献权重>`

---

## 五、父子链路与传导分析

| 层级 | 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 父指标 | 客数渗透率 | `<value>` | `<threshold: >=30%>` | `<mom>` | `<yoy>` | `<毛利率变化对客数渗透率的影响>` |
| 当前指标 | 全链路毛利率 | `<value>` | `<threshold: >27.9%>` | `<mom>` | `<yoy>` | `<利润质量判断>` |
| 子指标 | 门店毛利率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<对全链路毛利率的传导>` |
| 子指标 | 供应链毛利率 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<对全链路毛利率的传导>` |

### 传导诊断

- `<判断全链路毛利率变化如何影响客数渗透率的利润质量>`
- `<说明全链路毛利率与客数渗透率之间是否存在背离，如客数渗透率上升而毛利率下降等情况>`
- `<门店毛利率与供应链毛利率的联动关系分析>`

---

## 六、区域表现拆解

| 区域 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area_name>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于全国均值、阈值达成判断>` |

### 区域结构诊断

- `<全链路毛利率最高的区域及其特征（如粤东、粤西等高毛利区域），最低的区域及其拖累因素>`
- `<区域间分化是否在扩大，是否存在显著的区域间毛利率差距>`
- `<如有时序比较，指出哪些区域在改善、哪些在恶化>`

---

## 七、品类表现拆解

| 品类 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<category_name>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于整体均值、对整体毛利率的贡献判断>` |

### 品类结构诊断

- `<全链路毛利率最高的品类及其特征（如猪肉、冷藏加工等高毛利品类），最低的品类及其特征>`
- `<品类间毛利率差距是否合理，是否存在结构性短板品类>`
- `<如有时序比较，指出哪些品类毛利率在改善、哪些在恶化>`

---

## 八、核心问题诊断与根因分析

### 1. `<问题一：来自 CLI 的全链路毛利率核心症状>`

- 现象：`<数值、阈值、同比、环比、排名或异常点>`
- 影响：`<对客数渗透率利润质量、门店盈利或供应链盈利的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的门店/供应链毛利率结构性拖累>`

- 现象：`<门店毛利率或供应链毛利率的异常、区域/品类分化或排名异常>`
- 影响：`<对全链路毛利率的拖累幅度及两侧贡献权重变化>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 3. `<问题三：来自 CLI 的趋势风险或区域/品类结构异常>`

- 现象：`<趋势拐点、连续异常日、波动率变化或区域/品类集中度变化>`
- 影响：`<对短期和中期全链路毛利率走势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

---

## 九、优化策略与行动建议

### 1. 整体毛利率优化

- 短期动作：`<针对全链路毛利率整体水平和阈值达成证据提出动作，必须回指第八章问题>`
- 长期动作：`<毛利率结构优化、利润质量管理机制>`
- 跟踪指标：全链路毛利率、客数渗透率

### 2. 门店毛利优化

- 短期动作：`<针对门店毛利率证据提出动作>`
- 长期动作：`<门店定价策略、成本管控、销售结构优化>`
- 跟踪指标：全链路毛利率、门店毛利率

### 3. 供应链毛利优化

- 短期动作：`<针对供应链毛利率证据提出动作>`
- 长期动作：`<供应链成本管理、集采策略、仓储配送效率优化>`
- 跟踪指标：全链路毛利率、供应链毛利率

### 4. 区域/品类结构优化

- 短期动作：`<针对低毛利率区域或品类证据提出动作>`
- 长期动作：`<区域差异化定价、品类结构优化机制>`
- 跟踪指标：全链路毛利率、区域排名、品类排名

---

## 十、风险提示与后续跟踪

- 风险提示：`<只列由 CLI 证据支持的全链路毛利率、门店/供应链毛利或结构风险>`
- 后续跟踪：`<明确后续应跟踪的已返回指标、区域、品类或时间窗口>`
- 重点关注：`<需要持续监控的指标阈值、区域或品类>`

---

## 十一、附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| 客数渗透率 | 全链路毛利率的父级指标，用户覆盖与触达能力的总纲指标 |
| 全链路毛利率 | 门店毛利与供应链毛利综合后的到店毛利率（不含税），反映整体经营效率 |
| 门店毛利率 | 门店端的毛利率，销售收入减去销售成本后占销售收入的比例 |
| 供应链毛利率 | 仓库配送到门店的商品毛利率（不含税），反映供应链环节的盈利水平 |