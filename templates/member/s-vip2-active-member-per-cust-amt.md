# vip2活跃会员客单价指标分析报告模板

> 说明：本模板用于用户报表下的 vip2活跃会员客单价指标专项分析报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符。

## 通用填充规则

- vip2活跃会员客单价是用户规模与分层结构维度下"会员分层活跃与价值指标"组的叶子指标。
- 父指标：vip2活跃会员数（`vip2ActiveMemberNum`）。无子指标。
- 本报告核心问题是判断 vip2活跃会员客单价当前水平是否合理，同比环比变化方向及幅度，变化是否来自区域结构分化或趋势异常。
- 用户报表不支持品类过滤；不输出品类排名、品类拖累或品类贡献。
- 诊断必须基于已返回数据，使用"现象 -> 影响 -> 推断"的逻辑。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report user` 返回结果，围绕 **vip2活跃会员客单价** 在用户规模与分层结构中的整体表现、趋势变化、区域结构分化和可行动优化方向进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area filters or 全国不含港澳>`
- 数据来源：`report user indicators area trend`
- 总体判断：`<基于 CLI 证据概括 vip2活跃会员客单价当前值、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕 vip2活跃会员客单价当前值、同比环比变化方向和幅度>`
2. `<结论二：围绕区域分化，指出客单价主要领先/落后的区域>`
3. `<结论三：围绕趋势异常或结构性风险>`

---

## 三、vip2活跃会员客单价整体表现

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| vip2活跃会员客单价 | `<value>元` | `<无阈值>` | `<mom>` | `<yoy>` | `<diagnosis>` |

### 客单价判断

- `<vip2活跃会员客单价当前水平评估，与其他 VIP 层级客单价对比>`

### 趋势表现

- `<使用 trend 返回的最近周期变化方向、峰谷或异常点证据>`

### 区域结构

- 区域表现：`<使用 area 返回的排名前/后区域及其同比环比证据>`

---

## 四、区域表现拆解

| 区域 | 客单价 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area_name>` | `<value>元` | `<mom>` | `<yoy>` | `<高于/低于全国均值、消费价值判断>` |

### 区域结构诊断

- `<vip2活跃会员客单价最高的区域及其驱动因素判断>`
- `<区域间分化是否在扩大>`

---

## 五、核心问题诊断与根因分析

### 1. `<问题一：来自 CLI 的 vip2活跃会员客单价核心症状>`

- 现象：`<数值、同比、环比、排名或异常点>`
- 影响：`<对 VIP2 会员消费价值或利润率的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的区域客单价分化>`

- 现象：`<区域分化、排名异常>`
- 影响：`<对整体客单价的拖累幅度>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 3. `<问题三：来自 CLI 的趋势风险或异常波动>`

- 现象：`<趋势拐点、连续异常日>`
- 影响：`<对短期和中期客单价走势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

---

## 六、优化策略与行动建议

### 1. VIP2 会员客单价提升

- 短期动作：`<针对客单价证据提出动作，必须回指第五章问题>`
- 长期动作：`<品类升级引导、组合推荐、定价策略优化等机制>`
- 跟踪指标：vip2活跃会员客单价、vip2活跃会员消费频次

### 2. 区域客单价差异化优化

- 短期动作：`<针对区域客单价分化证据提出动作>`
- 长期动作：`<区域差异化定价策略、低客单价区域消费升级引导机制>`
- 跟踪指标：vip2活跃会员客单价、vip2活跃会员数

### 3. VIP2 会员消费价值增强

- 短期动作：`<针对客单价趋势下滑证据提出动作>`
- 长期动作：`<VIP2 向 VIP3 升级转化激励、高价值商品推荐机制>`
- 跟踪指标：vip2活跃会员客单价、vip3活跃会员数

---

## 七、风险提示与后续跟踪

- 风险提示：`<只列由 CLI 证据支持的客单价下滑、区域分化扩大或趋势异常风险>`
- 后续跟踪：`<明确后续应跟踪的已返回指标、区域或时间窗口>`
- 重点关注：`<需要持续监控的客单价拐点、区域或趋势异常>`

---

## 八、附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| vip2活跃会员客单价 | 会员等级为vip2的活跃会员产生的订单的平均销售额；统计逻辑为 vip2活跃会员销售额 / vip2活跃会员来客数 |
| vip2活跃会员数 | 会员等级为VIP2的活跃会员id去重计数，反映中等级VIP用户规模 |
| vip2活跃会员消费频次 | VIP2活跃会员的平均消费次数，反映中等级VIP消费活跃度 |
| 活跃用户数 | 有消费行为的用户id去重计数，反映整体用户规模；单位为万 |