# vip3活跃会员客单价指标分析报告模板

> 说明：本模板用于用户报表下vip3活跃会员数树中的vip3活跃会员客单价专项报告。所有数值、同比、环比、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- 本指标为叶子指标，无下钻子指标，不展示父子链路章节。
- 本报告核心问题是判断vip3活跃会员客单价的变化驱动因素（高价值商品消费倾向、促销折扣影响、区域价格结构差异），并说明对vip3层级会员消费价值的含义。
- vip3活跃会员客单价固定归入"用户规模与分层结构"维度（第三章），不得放入第四章（用户价值与复购转化）或第五章（用户触达与渠道效率）。
- 区域、趋势证据只用于解释vip3活跃会员客单价的表现，不扩展为用户总览报告。
- 用户报表不支持品类过滤，报告中不涉及品类维度分析。
- CLI 返回 null 或缺失区域时，只能按 CLI 事实描述为数据缺口，不得自行补数。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report user` 返回结果，围绕**vip3活跃会员客单价**在用户报表中的表现、区域分化和趋势风险进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area filters or 全局>`
- 数据来源：`report user indicators/area/trend`
- 总体判断：`<基于 CLI 证据概括vip3活跃会员客单价当前值、同比、环比和主要变化方向>`

## 二、核心结论

1. `<结论一：围绕vip3活跃会员客单价整体变化，引用当前值、同比或环比证据>`
2. `<结论二：结合区域证据，说明各区域客单价的分化情况和主要拖累/拉动区域>`
3. `<结论三：结合趋势证据，说明客单价的月度走势特征、波动幅度和异常点>`

## 三、整体表现与趋势

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| vip3活跃会员客单价 | `<value>元` | `<threshold or 无>` | `<mom>` | `<yoy>` | `<diagnosis>` |

- 趋势表现：`<使用 trend 返回的最近周期、峰谷、连续变化或异常点证据>`
- 消费价值判断：`<说明vip3活跃会员客单价变化反映的消费升级/降级趋势和会员价值变化>`

## 四、区域表现拆解

| 区域 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area>` | `<value>` | `<mom>` | `<yoy>` | `<diagnosis>` |

- 区域分化特征：`<基于区域排名说明客单价的高值区域和低值区域分化>`
- 区域风险提示：`<指出环比或同比异常的区域及可能原因，如促销力度差异、商品结构差异>`

## 五、核心问题诊断与根因推断

### 1. `<问题一：来自 CLI 的vip3活跃会员客单价核心症状>`

- 现象：`<数值、同比、环比或异常点>`
- 影响：`<对vip3层级会员消费价值和高客单转化能力的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的区域性或趋势异常>`

- 现象：`<区域或趋势异常>`
- 影响：`<对vip3会员客单价稳定性和消费升级趋势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

## 六、优化策略与行动建议

- 客单价提升：`<针对vip3活跃会员客单价证据提出动作，必须回指第五章问题>`
- 区域价格策略：`<针对高客单/低客单区域差异化提出商品组合或促销策略>`
- 趋势监控：`<围绕趋势异常点提出持续跟踪指标>`
- 跟踪指标：`<从 CLI 已返回指标中选择，如 vip3ActiveMemberPerCustAmt、vip3ActiveMemberNum 等>`

## 七、附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| vip3活跃会员客单价 | vip3活跃会员产生的订单的平均销售额 |
| vip3活跃会员数 | 会员等级为vip3且统计周期内有消费行为的去重会员数 |
| vip3活跃会员消费频次 | vip3活跃会员平均消费次数 |