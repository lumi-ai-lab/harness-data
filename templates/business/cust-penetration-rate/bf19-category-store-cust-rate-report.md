# 19点前PI值指标分析报告模板

> 说明：本模板用于经营分析下客数渗透率树中的 19点前PI值专项报告。所有数值、同比、环比、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- 固定拆解链路：`客数渗透率 -> 客数 -> 19点前客数 -> 19点前PI值`。
- 本报告核心问题是判断 19点前PI值的早时段触达/结构效率，并说明它对 19点前客数、客数和客数渗透率的传导影响。
- 19点前客数、客数和客数渗透率只用于父级传导，不能替代 19点前PI值主指标。
- 若 CLI 返回 1、100% 或全区域持平等边界值，必须按 CLI 事实谨慎描述，不得自行解释为业务原因。
- 区域、品类、趋势证据只用于解释 19点前PI值结构分化和异常，不扩展为经营总览报告。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report business` 返回结果，围绕**19点前PI值**在客数渗透率树中的表现、父级传导、区域品类分化和趋势稳定性进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area/category/indicator filters or 全局>`
- 数据来源：`report business indicators/tree/area/category/trend`
- 总体判断：`<基于 CLI 证据概括19点前PI值当前值、同比、环比、阈值和主要变化方向>`

## 二、核心结论

1. `<结论一：围绕19点前PI值当前值、同比或环比给出主判断>`
2. `<结论二：结合19点前客数、客数和客数渗透率，说明触达效率对父级指标的传导>`
3. `<结论三：结合区域、品类或趋势证据，说明结构稳定、改善来源或风险>`

## 三、父级链路与传导判断

| 层级 | 指标 | 当前值 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 上级指标 | 客数渗透率 | `<value>` | `<mom>` | `<yoy>` | `<父级结果判断>` |
| 上级指标 | 客数 | `<value>` | `<mom>` | `<yoy>` | `<客流规模传导判断>` |
| 父指标 | 19点前客数 | `<value>` | `<mom>` | `<yoy>` | `<早时段客流传导判断>` |
| 当前指标 | 19点前PI值 | `<value>` | `<mom>` | `<yoy>` | `<早时段触达效率判断>` |

## 四、19点前PI值整体表现与趋势

| 指标 | 当前值 | 阈值 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 19点前PI值 | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<diagnosis>` |

- 趋势表现：`<使用 trend 返回的最近周期、峰谷、连续变化或异常点证据>`
- 父级影响：`<说明19点前PI值变化如何影响19点前客数、客数和客数渗透率>`

## 五、区域与品类分化

### 区域表现

| 区域 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area>` | `<value>` | `<mom>` | `<yoy>` | `<diagnosis>` |

### 品类表现

| 品类 | 当前值 | 阈值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `<category>` | `<value>` | `<threshold>` | `<mom>` | `<yoy>` | `<diagnosis>` |

## 六、核心问题诊断与根因推断

### 1. `<问题一：来自 CLI 的19点前PI值核心症状>`

- 现象：`<数值、阈值、同比、环比、排名或异常点>`
- 影响：`<对19点前客数、客数或客数渗透率的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的结构性拖累或风险>`

- 现象：`<区域、品类、趋势异常或边界值>`
- 影响：`<对早时段触达和父指标传导的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

## 七、行动建议与跟踪指标

- 早时段触达改善：`<针对19点前PI值、区域或品类证据提出动作，必须回指第六章问题>`
- 客流承接：`<针对19点前客数证据提出动作>`
- 跟踪指标：`<从 CLI 已返回指标中选择>`

## 八、附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| 客数渗透率 | 客数所属的父级经营分析一级指标 |
| 客数 | 19点前客数所属的父级客流规模指标 |
| 19点前客数 | 19点前PI值所属的父级早时段客流规模 |
| 19点前PI值 | 早时段品类或商品触达效率指标 |
