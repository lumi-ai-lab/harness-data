# 门店毛利额指标分析报告模板

> 说明：本模板用于经营分析下客数渗透率树中的门店毛利额专项分析报告。所有数值、同比、环比、阈值、排名、异常点、诊断事实和根因判断必须来自 CLI 输出。
> CLI 未返回的指标行、指标组或段落直接省略，不写缺失说明，不保留占位符，不使用本地 demo 数据或静态示例值。

## 通用填充规则

- 门店毛利额是客数渗透率维度下全链路盈利组的叶子指标，归入用户渗透链路中的门店结果指标。
- 固定拆解链路：`客数渗透率 -> 全链路毛利额 -> 门店毛利额`。
- 本报告核心问题是判断门店侧毛利额当前水平是否达标（阈值 >=2952.12），同比环比变化方向及幅度，变化是否来自区域分化或品类结构偏移，以及门店毛利额变化对全链路毛利额和客数渗透率的传导影响。
- 全链路毛利额和客数渗透率只用于父级传导，供应链毛利额只作为同组补充，不替代当前指标。
- 区域、品类、趋势证据只用于解释门店毛利额结构分化和异常，不扩展为经营总览报告。
- 全链路毛利率、门店毛利率、供应链毛利率、销售额、客数、客单价、19点前链路指标不得作为本报告主指标行。
- 禁放：第四章（品效维度）、第五章（供应链维度）。
- 诊断必须基于已返回数据，使用"现象 -> 影响 -> 推断"的逻辑，不把通用业务常识写成已发生事实。
- 根因必须受 CLI 证据约束；CLI 只支持趋势或结构异常时，只能写"可能""倾向于"。
- 门店毛利额 `valueUnit: 2`，值为金额直接使用（如 4114.52），同比环比 `unit` 为 2（比率变化）。
- 门店毛利额阈值 `compareSymbol: "GE"`，`compareValue1: 2952.12`，表示需要 >=2952.12。

---

## 一、报告概述

本报告基于 `<time_label>` 的 `qdm-cmr-cli report business` 返回结果，围绕**门店毛利额**在客数渗透率树中的表现、阈值达成、父级传导、区域品类分化和趋势变化进行专项分析。

- 时间口径：`<date/week/month>`
- 筛选口径：`<area/category/indicator filters or 全局>`
- 数据来源：`report business indicators area category trend`
- 总体判断：`<基于 CLI 证据概括门店毛利额当前值、阈值达成、同比、环比和主要变化方向>`

---

## 二、核心结论

1. `<结论一：围绕门店毛利额当前值、是否达标（>=2952.12）、同比环比变化方向和幅度>`
2. `<结论二：结合全链路毛利额和客数渗透率说明门店端毛利额对父级指标的传导影响>`
3. `<结论三：围绕区域和品类分化，指出主要领先/拖累的区域和品类，或趋势风险点>`

---

## 三、父级链路与传导判断

| 层级 | 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 传导判断 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 上级指标 | 客数渗透率 | `<value>` | `>=30%` | `<mom>` | `<yoy>` | `<父级结果判断>` |
| 父指标 | 全链路毛利额 | `<value>` | 无预设阈值 | `<mom>` | `<yoy>` | `<全链路毛利额传导判断>` |
| 当前指标 | 门店毛利额 | `<value>` | `>=2952.12` | `<mom>` | `<yoy>` | `<门店端毛利额判断>` |
| 同组补充 | 供应链毛利额 | `<value>` | 无预设阈值 | `<mom>` | `<yoy>` | `<供应链侧补充判断>` |

### 传导诊断

- `<判断门店毛利额变化对全链路毛利额的贡献方向和幅度>`
- `<判断门店毛利额在客数渗透率链路中的位置和影响>`
- `<对比门店毛利额与供应链毛利额的变化方向和幅度差异>`

---

## 四、门店毛利额整体表现与趋势

| 指标 | 当前值 | 阈值/目标 | 环比变动 | 同比变动 | 关键诊断 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 门店毛利额 | `<value>` | `>=2952.12` | `<mom>` | `<yoy>` | `<diagnosis>` |

### 阈值判断

- `<是否达到阈值（>=2952.12），未达标时说明差距幅度>`
- `<与上期对比的达标情况变化>`

### 趋势表现

- `<使用 trend 返回的最近周期变化方向（持续上升/下滑/震荡）、峰谷或异常点证据>`
- `<近30天趋势中是否存在显著拐点或连续多日偏离均值的情况>`

### 父级影响

- `<说明门店毛利额变化如何影响全链路毛利额和客数渗透率>`

---

## 五、区域与品类分化

### 区域表现

| 区域 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<area_name>` | `<value>` | `<mom>` | `<yoy>` | `<高于/低于全国均值、阈值达成判断>` |

### 区域结构诊断

- `<门店毛利额最高的区域及其驱动因素判断，最低的区域及其拖累因素判断>`
- `<区域间分化是否在扩大，是否存在显著区域间门店毛利额差距>`
- `<如有时序比较，指出哪些区域在改善、哪些在恶化>`

### 品类表现

| 品类 | 当前值 | 环比变动 | 同比变动 | 诊断 |
| :--- | :--- | :--- | :--- | :--- |
| `<category_name>` | `<value>` | `<mom>` | `<yoy>` | `<对整体门店毛利额的贡献判断>`

### 品类结构诊断

- `<门店毛利额最高的品类及其特征（高毛利/高销售额驱动），最低的品类及其特征>`
- `<品类间门店毛利额差距是否合理，是否存在结构性短板品类>`
- `<如有时序比较，指出哪些品类门店毛利额在改善、哪些在恶化>`

---

## 六、核心问题诊断与根因推断

### 1. `<问题一：来自 CLI 的门店毛利额核心症状>`

- 现象：`<数值、阈值、同比、环比、排名或异常点>`
- 影响：`<对全链路毛利额或客数渗透率的传导影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 2. `<问题二：来自 CLI 的区域或品类结构性拖累>`

- 现象：`<区域/品类分化、排名异常或集中度变化>`
- 影响：`<对整体门店毛利额的拖累幅度>`
- 推断：`<基于 CLI 证据的有限根因判断>`

### 3. `<问题三：来自 CLI 的趋势风险或异常波动>`

- 现象：`<趋势拐点、连续异常日、波动率变化>`
- 影响：`<对短期和中期门店毛利额走势的影响>`
- 推断：`<基于 CLI 证据的有限根因判断>`

---

## 七、行动建议与跟踪指标

### 1. 门店毛利额修复

- 短期动作：`<针对门店毛利额、区域或品类证据提出动作，必须回指第六章问题>`
- 长期动作：`<门店端销售结构优化、高毛利品类推广、区域差异化管理等机制>`
- 跟踪指标：门店毛利额、全链路毛利额、供应链毛利额

### 2. 全链路毛利额承接

- 短期动作：`<针对全链路毛利额证据提出动作>`
- 长期动作：`<门店+供应链协同优化机制>`
- 跟踪指标：全链路毛利额、门店毛利额、供应链毛利额

### 3. 区域与品类风险管控

- 短期动作：`<针对区域或品类异常证据提出动作>`
- 长期动作：`<区域差异化目标、品类毛利额分层管理机制>`
- 跟踪指标：门店毛利额（分区域）、门店毛利额（分品类）

---

## 附录：指标定义说明

| 指标 | 核心定义 |
| :--- | :--- |
| 客数渗透率 | 门店毛利额所属的经营分析一级指标（用户渗透维度），用户覆盖与触达能力的总纲指标 |
| 全链路毛利额 | 门店毛利额所属的父级毛利额指标，全链路利润规模 |
| 门店毛利额 | 销售收入减去销售成本（销售额-(进货额+门店期初库存金额-门店期末库存金额)），归入用户渗透链路中的门店结果指标 |
| 供应链毛利额 | 门店毛利额的同组补充指标，反映仓储环节对利润的贡献 |