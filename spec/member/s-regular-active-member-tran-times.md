---
id: member-s-regular-active-member-tran-times
kind: spec
domain: member
title: 普通活跃会员消费频次指标详情
tags:
  - report
  - metric
  - user-report
  - regularActiveMemberTranTimes
  - 普通活跃会员消费频次
match:
  keywords:
    - 普通活跃会员消费频次
    - 普通活跃会员消费频次指标
    - 普通活跃会员消费频次详情
    - regularActiveMemberTranTimes
---

# 普通活跃会员消费频次指标详情

> 数据来源：`qdm-cmr-cli indicator detail --code regularActiveMemberTranTimes --full`

## 基本信息

| 属性 | 值 |
| :--- | :--- |
| 指标中文名 | 普通活跃会员消费频次 |
| 指标英文 code | `regularActiveMemberTranTimes` |
| 业务定义 | 会员等级为普通会员的活跃会员，平均每位会员在统计周期内的消费次数 |
| 统计逻辑 | 统计周期内，普通活跃会员来客数 / 普通活跃会员消费人数 |
| 业务环节 | 用户运营 |

## 指标定位

- 普通活跃会员消费频次是用户运营 `/report/3` 普通活跃会员数的子指标，属于用户规模与分层结构维度。
- 所属维度：用户规模与分层结构维度。
- 报告章节：第三章 用户规模与分层结构维度深度拆解。
- 指标组：会员分层活跃与价值指标。
- 固定拆解链路：`活跃用户数 -> 普通活跃会员数 -> 普通活跃会员消费频次`。

## 下钻子指标

该指标为叶子指标，无子指标。

## 边界与禁放规则

- 用户报表不支持品类过滤；不输出品类排名、品类拖累或品类贡献。
- 普通活跃会员消费频次归入用户规模与分层结构维度，固定放入第三章，不得放入第四章（会员价值与复购转化）和第五章（用户触达与渠道效率）。
- 该指标为普通活跃会员数的质量指标，在报告中紧跟普通活跃会员数展示。
- 所有数值、同比、环比、排名、异常和根因必须来自 CLI 输出；CLI 未返回的指标行直接省略。
- 任何指标只能放入其对应维度的主章节，禁止跨维度混放。