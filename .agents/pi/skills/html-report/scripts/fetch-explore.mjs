/**
 * STUB — Fetch explore data for B3 Researcher new_query tasks.
 *
 * This file is a placeholder for Issue #37 (B0_PREFLIGHT). It exports only the
 * symbols imported by check-session-layout.mjs. The full implementation (874
 * lines) will be ported from the reference workspace when Issue #39
 * (B3_RESEARCH) is implemented.
 *
 * Reference: /Users/pengmd/c/qdm/harenss-data-github-ppt-master/.agents/pi/skills/html-report/scripts/fetch-explore.mjs
 */

/**
 * Compute material query delta between original requestBody and explore payload.
 * Full implementation compares indicator/dim/date/filter fields and returns a
 * structured delta object. Stub returns null — only called in explore/quality/html phases.
 */
export function materialQueryDelta() {
  return null;
}

/**
 * Extract the semantic shape of a query payload.
 * Full implementation returns a normalized shape descriptor for comparison.
 */
export function semanticQueryShape() {
  return null;
}
