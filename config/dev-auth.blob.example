# Working copy for local authz (gitignored).
# Codex Plugin Setup normally writes the supplied blob to:
#   <pluginRoot>/secrets/auth.blob
#
# Development fixture source:
#   config/fixtures/local-test-auth.blob
#
# Production users may provide --auth-blob-file or HARNESS_AUTH_BLOB_FILE.
# Keep the file outside the workspace and use permissions 0600 when possible.
# Do not commit a real auth.blob.
