# Fallback restore when no baseline exists (switch.sh prefers baseline).
# Prefer: bash config/authz-manual-test/switch.sh restore
paths:
  knowledge: wikis

cli:
  qdm_metric_cli: bin/qdm-metric-cli
  qdm_sql_cli: bin/qdm-sql-cli
  qdm_cas_cli: bin/cas-cli

authz:
  mode: off
  allow_local_blob: true
