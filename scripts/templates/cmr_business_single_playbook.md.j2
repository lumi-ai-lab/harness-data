---
name: "{{ frontmatter_name }}"
label: "{{ label }}取数手册"
intents:
  current_value:
    aliases:
      - 值
      - 指标值
      - 是多少
      - 多少
      - 查看
      - 查询
      - 看一下
  trend:
    aliases:
      - 趋势
      - 走势
      - 趋势分析
  area_performance:
    aliases:
      - 区域表现
      - 区域排名
      - 区域对比
      - 哪个区域高
      - 哪个区域低
  category_performance:
    aliases:
      - 品类表现
      - 品类排名
      - 品类对比
      - 哪个品类高
      - 哪个品类低
---

# {{ label }}取数手册

## 适用问题

用户询问{{ label }}{{ alias_phrase }}或 `{{ code }}` 的当前值、趋势、区域表现或品类表现时，使用本手册。

{{ label }}属于 CMR 经营分析报表。当前值使用新版 CMR CLI 的 `dupont` 命令查询；趋势使用 `trend` 命令查询；区域表现使用 `area` 命令查询；品类表现使用 legacy `report business category` 命令查询。本手册只处理围绕{{ label }}的取数，不处理原因归因、综合报告或指标关系判断。

## 默认查询口径

当用户没有指定任何过滤条件时，默认查询：

- 报表：经营分析，`--report business`
- 指标：{{ label }}，`--indicator-name {{ label }}`
- 时间：昨天，按 `spec/common/time-policy.md` 解析为 `--date <YYYY-MM-DD>`
- 区域：CLI 默认区域，即 `管理区域 / CN00 / 全国(不含港澳)`
- 品类：CLI 默认品类，即 `大分类 / 00 / 全品类`

默认当前值查询命令：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --date <YYYY-MM-DD>
```

最终回答中必须说明口径是“全国、全品类”。不要把文档示例中的数值当成真实结果。

## 查询入口选择

根据用户问题选择一个取数入口。用户只问“{{ label }}是多少”“{{ label }}的值”“看一下{{ label }}”时，默认查询当前值。

| 用户意图 | CLI 命令 | 说明 |
| --- | --- | --- |
| 当前值、指标值、是多少 | `dupont` | 查询满足过滤条件后的{{ label }}值 |
| 趋势、走势、最近变化 | `trend` | 查询{{ label }}按时间展开的趋势点 |
| 区域表现、区域排名、哪个区域高或低 | `area` | 查询{{ label }}按区域展开的表现 |
| 品类表现、品类排名、哪个品类高或低 | `report business category` | 查询{{ label }}按品类展开的表现 |

不要因为用户问趋势、区域表现或品类表现就切换到归因分析；这些问题仍然是{{ label }}这个单指标在不同维度下的取数。

当前值查询必须采用最小取数路径：只执行用户显式请求口径对应的 `dupont` 命令。用户没有明确要求同比、环比、趋势、区域排名、品类排名、原因或额外对比时，不要查询历史同期/上期，不要查询用户未提到的区域或品类，也不要为了验证重复执行同一口径命令。

## 时间过滤

时间参数只能使用日、周、月三种之一，不要组合多个时间参数，也不要用日期范围模拟周或月。

| 用户表达 | CLI 参数 | 说明 |
| --- | --- | --- |
| 未指定时间 | `--date <昨天>` | 默认查昨天 |
| 今天、今日 | `--date <current_date>` | 使用 hook 提供的当前日期 |
| 昨天、昨日 | `--date <current_date - 1>` | 使用当前日期前一日 |
| 具体日期 | `--date YYYY-MM-DD` | 按用户给定日期 |
| 本周、这周、最近一周 | `--week <当前业务周>` | 业务周用 `search weeks` 辅助确认 |
| 上周 | `--week <上一业务周>` | 取 `search weeks` 中当前周后一条 |
| 本月、这个月 | `--month YYYY-MM` | 当前月份 |
| 上月 | `--month YYYY-MM` | 上一月份 |

示例：查询 2026-05-28 全国全品类{{ label }}：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --date 2026-05-28
```

示例：查询 2026-20 周全国全品类{{ label }}：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --week 2026-20
```

示例：查询 2026-05 月全国全品类{{ label }}：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --month 2026-05
```

## 区域过滤

用户指定区域、大区、督导、门店范围时，需要增加区域过滤。区域维度和值必须以 CLI 可识别值为准；不确定时先用 `search stores` 查询。

区域名称数字归一化规则：

- 管理区域、大区、门店名称中出现阿拉伯数字时，查询前必须转换为中文数字。
- 例如用户说“粤东1区”，实际查询应使用“粤东一区”。
- 这个规则也适用于类似“粤西2区”“长沙2区”“广州XX第2店”等区域或门店名称；优先将名称归一化后再执行 `search stores` 或 `dupont`。
- 如果归一化后仍无法确认编码，先用对应区域维度执行 `search stores`，不要自行猜测编码。

常用区域过滤参数：

| 用户表达 | CLI 参数 |
| --- | --- |
| 全国、全国不含港澳 | 不额外传区域参数，使用默认 `管理区域 / CN00` |
| 某管理区域，如粤西区 | `--area-type 管理区域 --area 粤西区` |
| 某区域编码，如 CN01 | `--area-type 管理区域 --area CN01` |
| 某大区，如粤东一区 | `--area-type 大区 --area 粤东一区` |
| 某督导，如张龙 | `--area-type 督导 --area 张龙` |
| 某门店，如广州华景北店 | `--area-type 门店 --area 广州华景北店` |
| 不确定区域名或编码 | 先执行 `search stores --type <区域维度> --keyword <关键词>` |

搜索区域示例：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" search stores --type 管理区域 --keyword 粤西区
```

查询大区“粤东一区”的{{ label }}：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --date 2026-05-28 --area-type 大区 --area 粤东一区
```

如果用户问“粤东一区下面有哪些维度”“粤东一区下一级是什么”“继续下钻粤东一区”，这不是指标表现查询。先确认父级 ID，再使用 `search stores child-list --parent <父级ID>` 查询下一级维度清单。

## 品类过滤

用户指定大分类、品类、商品分类时，需要增加品类过滤。品类维度和值必须以 CLI 可识别值为准；不确定时先用 `search categories` 查询。

常用品类过滤参数：

| 用户表达 | CLI 参数 |
| --- | --- |
| 全品类 | 不额外传品类参数，使用默认 `大分类 / 00` |
| 某大分类，如水果 | `--category-type 大分类 --category 水果` |
| 某大分类编码，如 12 | `--category-type 大分类 --category 12` |
| 某中分类，如热带果类 | `--category-type 中分类 --category 热带果类` |
| 某中分类编码，如 1206 | `--category-type 中分类 --category 1206` |
| 不确定品类名或编码 | 先执行 `search categories --type <品类维度> --keyword <关键词>` |

搜索品类示例：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" search categories --type 大分类 --keyword 水果
```

如果用户问“大分类下面有哪些中分类”“水果下一级品类是什么”“继续查看下级品类”，这不是指标表现查询。先确认父级 ID，再使用 `search categories child-list --parent <父级ID>` 查询下级品类清单。

查询水果品类的全国{{ label }}：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --date 2026-05-28 --category-type 大分类 --category 水果
```

## 组合过滤

时间、区域、品类可以组合使用，但每类过滤只保留一个明确口径。

查询粤西区、水果品类、2026-05-28 的{{ label }}：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --date 2026-05-28 --area-type 管理区域 --area 粤西区 --category-type 大分类 --category 水果
```

## 当前值查询

当前值查询统一使用 `dupont`，并把用户明确给出的时间、区域、品类过滤条件传入命令。

当前值问题中的多个口径要逐个按当前值查询处理：

- 用户问“全国和粤西区的{{ label }}”，执行全国默认口径和粤西区口径两条 `dupont`，不要改用 `area`。
- 用户问“粤东区和粤西区的{{ label }}”，只执行这两个区域口径的 `dupont`。
- 用户问“全国和水果品类的{{ label }}”，只执行用户明确指定的当前值口径；如果语义是全国水果品类，则执行一条带品类过滤的 `dupont`。
- 只有区域名、品类名或编码无法确认时，才先执行对应 `search stores` 或 `search categories`；确认后仍只查询用户显式口径。

查询默认口径的{{ label }}当前值：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" dupont --report business --indicator-name {{ label }} --date 2026-05-28
```

## 趋势查询

用户询问“{{ label }}趋势”“{{ label }}走势”“最近{{ label }}变化”时，使用 `trend`。趋势命令固定查询 `grouping=ctime`，仍然只围绕{{ label }}这一个指标取数。

查询全国全品类{{ label }}趋势：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" trend --report business --indicator-name {{ label }} --date 2026-05-28
```

趋势输出中只使用 CLI 返回的时间点和值。不要自行补齐缺失日期，也不要根据相邻点推算数值。

## 区域表现查询

用户询问“各区域{{ label }}”“哪个区域{{ label }}高”“哪个区域{{ label }}低”“区域排名”时，使用 `area`。区域表现命令固定查询 `grouping=storeId`，可以继续叠加时间和品类过滤。

如果用户只是要求某一个或多个明确区域的当前值，即使表达中包含“全国和某区域”，也不是区域表现查询；应使用 `dupont` 分别查询这些明确口径。只有用户要求区域展开、排名、最高/最低、各区域对比时才使用 `area`。

区域层级必须按 CLI 实际返回判断，不要只按用户原词或命令参数猜测：

- 不传 `--area-type/--area` 时，默认范围是 `管理区域 / CN00 / 全国(不含港澳)`，返回全国下一级管理区域表现。
- 用户说“区域”“管理区域”“大区/区域”但未明确到 `sapAreaId` 细分大区时，可以按默认管理区域排名回答，并在最终回答中写明“区域层级=管理区域”。
- 用户明确说“大区”且语义要求 `sapAreaId` 大区时，应按真正大区口径处理。
- 对层级有歧义或需要证明口径时，先用 `--full` 查看 `filters.storeTypeName`、`filters.areaName` 和返回 `code/name`。
- 如果当前 CLI 不能一条命令直接返回全国 `sapAreaId` 大区排名，应说明“当前 area 命令未直接返回全国 sapAreaId 大区层级排名”，不要用管理区域结果冒充大区结果。

查询全国全品类下的区域{{ label }}表现：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" area --report business --indicator-name {{ label }} --date 2026-05-28
```

按当前值从高到低查看区域表现：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" area --report business --indicator-name {{ label }} --date 2026-05-28 --sort current --order DESC
```

如果用户已经限定了某个区域，并要求查看该区域所在层级的{{ label }}当前值，应使用 `dupont`；如果要求该父级下面的下级排名，再用 `area` 指定目标层级和父级范围。

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" area --report business --indicator-name {{ label }} --date 2026-05-28 --area-type 督导 --area CN01 --sort current --order ASC
```

最终回答只能使用 `area` 返回的指标值，不要把 `child-list` 清单当成{{ label }}结果。用户要求 TOPN、排名、最低或最高时，不要逐个对子区域、督导或门店执行 `dupont` 后手工排序。

## 品类表现查询

用户询问“各品类{{ label }}”“哪个品类{{ label }}高”“哪个品类{{ label }}低”“品类排名”时，使用 `report business category`。当前 CMR CLI 没有新版顶层 `category` 命令，品类表现需要调用 legacy 经营分析品类命令。

如果用户只是要求某一个明确品类的当前值，例如“水果品类的{{ label }}”，应使用带品类过滤的 `dupont`，不要使用品类表现命令。只有用户要求品类展开、排名、最高/最低、各品类对比时才使用 `report business category`。

查询全国范围内的品类{{ label }}表现：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" report business category --date 2026-05-28 --indicator {{ label }}
```

按当前值从高到低查看品类表现：

```bash
source config/qdm-cli-paths.env
"$QDM_CMR_CLI" report business category --date 2026-05-28 --indicator {{ label }} --sort current --order DESC
```

如果用户要求在某个品类层级下继续查看下级品类表现，这是指标表现查询，不是下级清单查询。先用 `search categories child-list` 确认下一层品类 ID，再用下一层 `--category-type` 和任一下一层品类 ID 查询该层指标表现。

最终回答只能使用 `report business category` 返回的 `rows` 指标值，不要把 `child-list` 清单当成{{ label }}结果。

## 回复后的追问引导

回答{{ label }}查询结果后，可以给出 1-2 个后续追问建议。追问建议只能基于本手册已定义的能力生成，不要引导用户询问本手册未覆盖的归因、诊断、预测、经营建议或跨指标分析。

可引导的追问范围仅限：

- 当前值查询：使用 `dupont` 查询满足过滤条件后的{{ label }}值。
- 趋势查询：使用 `trend` 查询{{ label }}按时间展开的趋势点。
- 区域表现：使用 `area` 查询{{ label }}按区域展开的表现。
- 品类表现：使用 `report business category` 查询{{ label }}按品类展开的表现。
- 已支持过滤：时间、区域、品类。
- 下级区域清单：使用 `search stores child-list` 查询该区域下一级维度。
- 下级品类清单：使用 `search categories child-list` 查询该品类下一级分类。

生成追问建议时不要重复用户刚刚问过的同一查询入口，不推荐“为什么{{ label }}低”“{{ label }}受哪些因素影响”“怎么改善{{ label }}”“给经营建议”等本手册明确不处理的问题。

## 输出要求

- 当前值必须来自 `dupont` 输出中的 `value`。
- 趋势结果必须来自 `trend` 输出中的时间点和值。
- 区域表现必须来自 `area` 输出中的区域维度和值。
- 品类表现必须来自 `report business category` 输出中的品类维度和值。
- 下级区域清单必须来自 `search stores child-list` 输出中的 `value` 和 `label`。
- 下级品类清单必须来自 `search categories child-list` 输出中的 `value` 和 `label`。
- 指标名称以 CLI 输出中的 `indicatorName` 为准。
- 指标 code 以 CLI 输出中的 `indicatorCode` 为准。
- 过滤口径必须在最终回答中说明，包括时间、区域、品类。
- 不估算、不补造缺失值，不使用文档示例数值替代真实取数。
- CLI 返回空数组、null、`rows: []` 或无可用值时，直接说明未返回可用指标证据。
- 不把 `child-list` 返回的维度清单解释为{{ label }}指标结果。
- 单指标问题取数后直接回复指标值和必要口径，不执行 `bin/data-harness-cli inject-template`。
